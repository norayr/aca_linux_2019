fs devices
vfs
filesystems list in linux kernel
journaling filesystems
/dev/
/proc/

lsblk
cat /proc/partitions meminfo cpuifo
/proc/<pid>

mount, umount

mkfs,  


kernel filesystems.
userspace filsystems via fuse: flickrfs, ntfs-3g.

sync - dump buffers.

top and memory.


dd, mkswap, chmod, swapon, swapoff
